# IMAP 性能说明

当前 IMAP 仍主要服务于旧邮箱池与验证码链路。

## 1. 当前策略

- 保留连接池服务层
- 通过缓存和复用减少重复创建
- Outlook 增强能力尽量走 Graph / Protocol，不把新逻辑继续堆到 IMAP 上

## 2. 建议指标

- 活跃连接数
- 复用次数
- 创建次数
- 邮件缓存命中率

## 3. 建议

- 新工作台不要再围绕 IMAP 设计新功能
- IMAP 只保留在旧邮箱池和验证码链路

